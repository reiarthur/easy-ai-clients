# audio.transcribe(api="openrouter")

OpenRouter audio transcription through the official audio transcription route.

Environment variable: `OPENROUTER_API_KEY`

Cost uses provider response usage when available; otherwise it is marked
`unavailable`.

