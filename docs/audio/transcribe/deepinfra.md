# audio.transcribe(api="deepinfra")

DeepInfra OpenAI-compatible speech-to-text gateway.

Environment variable: `DEEPINFRA_API_KEY`

Default model: `openai/whisper-large-v3`

Cost is `unavailable` unless a provider response exposes usage/cost metadata.

