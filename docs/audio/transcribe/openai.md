# audio.transcribe(api="openai")

OpenAI speech-to-text through `POST /v1/audio/transcriptions`.

Environment variable: `OPENAI_API_KEY`

Default model: `gpt-4o-mini-transcribe`

Cost is estimated from OpenAI's published per-minute transcription pricing for
documented models.

