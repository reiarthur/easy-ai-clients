# audio.transcribe(api="google")

Gemini audio understanding used as a speech-to-text adapter.

Environment variable: `GOOGLE_API_KEY`

Default model: `gemini-2.5-flash`

Cost is `unavailable` because Gemini audio is billed as multimodal model usage
and the adapter does not infer USD cost without provider usage pricing.

