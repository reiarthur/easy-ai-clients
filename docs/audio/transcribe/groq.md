# audio.transcribe(api="groq")

Groq speech-to-text through its OpenAI-compatible transcription endpoint.

Environment variable: `GROQ_API_KEY`

Default model: `whisper-large-v3-turbo`

Cost is estimated from Groq's published hourly STT rates for documented models.

