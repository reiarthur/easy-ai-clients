# audio.transcribe(api="huggingface")

Hugging Face Inference Providers automatic speech recognition.

Environment variable: `HUGGINGFACE_API_KEY`

Default model: `openai/whisper-large-v3`

Cost is `unavailable` unless the selected provider returns a stable USD cost.

