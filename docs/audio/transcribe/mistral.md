# audio.transcribe(api="mistral")

Mistral Voxtral transcription through the Audio API.

Environment variable: `MISTRAL_API_KEY`

Default model: `voxtral-mini-latest`

Cost is `unavailable` unless Mistral exposes deterministic usage/cost metadata.

