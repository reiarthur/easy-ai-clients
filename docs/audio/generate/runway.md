# audio.generate(api="runway")

Runway speech and sound generation. Use `audio_type="speech"` for speech or
`audio_type="sound_effect"` for text-to-sound.

Environment variable: `RUNWAYML_API_SECRET`

Cost is estimated from Runway credits and the documented `1 credit = $0.01`
conversion when duration/character metadata is sufficient.

