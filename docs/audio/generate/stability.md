# audio.generate(api="stability")

Stable Audio text-to-audio wrapper.

Environment variable: `STABILITY_API_KEY`

Default model: `stable-audio-2.5`

The wrapper excludes deprecated Stable Video Diffusion paths and only targets the
current Stable Audio text-to-audio surface.

