# audio.generate(api="openrouter")

OpenRouter text-to-speech through the official audio speech route.

Environment variable: `OPENROUTER_API_KEY`

Default model: `openai/gpt-4o-mini-tts-2025-12-15`

Cost is `unavailable` unless OpenRouter returns usage/cost metadata for the
request.

