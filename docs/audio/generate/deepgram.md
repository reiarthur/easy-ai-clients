# audio.generate(api="deepgram")

Deepgram Aura text-to-speech via `POST /v1/speak`.

Environment variable: `DEEPGRAM_API_KEY`

Default model: `aura-2-thalia-en`

Cost fields are populated from the documented character table when available;
otherwise `cost_source="unavailable"`.

