# image.analyze(api="mistral")

Mistral vision/OCR analysis through chat completions with image content.

Environment variable: `MISTRAL_API_KEY`

Default model: `pixtral-large-latest`

Cost is unavailable unless the provider response returns usage/cost metadata.

