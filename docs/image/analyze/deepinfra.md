# image.analyze(api="deepinfra")

DeepInfra vision analysis through its OpenAI-compatible chat/vision endpoint.

Environment variable: `DEEPINFRA_API_KEY`

Default model: `meta-llama/Llama-3.2-11B-Vision-Instruct`

Cost uses provider response usage if present; otherwise unavailable.

