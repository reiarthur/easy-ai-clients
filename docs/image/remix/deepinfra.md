# image.remix(api="deepinfra")

DeepInfra reference-image remix using the image edit gateway.

Environment variable: `DEEPINFRA_API_KEY`

Default model: `black-forest-labs/FLUX-1-Kontext-dev`

Cost is unavailable when provider usage is not returned.

