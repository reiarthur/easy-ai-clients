# image.generate(api="runway")

Runway text-to-image task wrapper.

Environment variable: `RUNWAYML_API_SECRET`

Default model: `gen4_image`

Cost is unavailable unless Runway exposes task-level usage for the image job.

