# image.generate(api="deepinfra")

DeepInfra image generation through the OpenAI-compatible image generation
gateway.

Environment variable: `DEEPINFRA_API_KEY`

Default model: `black-forest-labs/FLUX-1-schnell`

Returns both `cust_usd` and `cost_usd`; cost is unavailable unless the provider
returns usage metadata.

