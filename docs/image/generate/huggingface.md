# image.generate(api="huggingface")

Hugging Face Inference Providers text-to-image wrapper.

Environment variable: `HUGGINGFACE_API_KEY`

Default model: `black-forest-labs/FLUX.1-schnell`

Cost is unavailable unless the selected provider returns stable USD metadata.

