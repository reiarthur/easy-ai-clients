# image.edit(api="huggingface")

Hugging Face image-to-image wrapper for prompt-guided editing.

Environment variable: `HUGGINGFACE_API_KEY`

Default model: `stabilityai/stable-diffusion-xl-refiner-1.0`

Cost is unavailable unless the selected provider returns stable USD metadata.

